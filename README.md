## Initial Setup 

will install chrome and the neccesary selenium thing

    .\install.ps1

if you have problems with the ps1 file you can use this

    Set-ExecutionPolicy RemoteSigned

this is to install the python libraries. could probably trim it down

    pip install -r requirements.txt

## Script Usage

    python newTCGPlayerScraper.py